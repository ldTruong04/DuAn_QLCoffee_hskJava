# 📚 Hướng Dẫn Triển Khai Thiết Kế Hiện Đại\n\n## ✨ Những Gì Đã Được Thêm\n\n### 1. Hệ Thống Màu & Chủ Đề (Theme System)\n**Tệp:** `ModernUITheme.java`\n\n```java\n// Sử dụng màu trong code\nbutton.setBackground(ModernUITheme.PRIMARY_COLOR);\nbutton.setForeground(ModernUITheme.TEXT_PRIMARY);\npanel.setBackground(ModernUITheme.BG_PRIMARY);\nlabel.setFont(ModernUITheme.FONT_HEADING);\n```\n\n### 2. Nút Hiện Đại (Modern Button)\n**Tệp:** `ModernButton.java`\n\n```java\n// Tạo nút xanh lá cây\nJButton addBtn = new ModernButton(\n    \"Thêm\", \n    ModernUITheme.SUCCESS_COLOR, \n    ModernUITheme.TEXT_PRIMARY\n);\n\n// Tạo nút đỏ\nJButton deleteBtn = new ModernButton(\n    \"Xóa\", \n    ModernUITheme.DANGER_COLOR, \n    ModernUITheme.TEXT_PRIMARY\n);\n\n// Tạo nút xanh dương\nJButton editBtn = new ModernButton(\n    \"Sửa\", \n    ModernUITheme.INFO_COLOR, \n    ModernUITheme.TEXT_PRIMARY\n);\n```\n\n**Đặc điểm:**\n-  Góc bo tròn 8px\n- ✅ Hiệu ứng hover (làm sáng khi di chuột)\n- ✅ Anti-aliasing cho giao diện mớn\n- ✅ Con trỏ chuột thay đổi\n- ✅ Kích thước chuẩn: 120x40px\n\n### 3. Trường Nhập Hiện Đại (Modern TextField)\n**Tệp:** `ModernTextField.java` & `ModernPasswordField.java`\n\n```java\n// Tạo trường nhập\nJTextField field = new ModernTextField(20);\n\n// Tạo trường mật khẩu\nJPasswordField pwdField = new ModernPasswordField(20);\n```\n\n**Đặc điểm:**\n- ✅ Nền xám nhạt\n- ✅ Khung xanh chàm khi focus\n- ✅ Hiệu ứng transition mượt\n- ✅ Font và padding chuẩn\n- ✅ Kích thước chuẩn: 250x40px\n\n### 4. Công Cụ Styling (Modern Styler)\n**Tệp:** `ModernStyler.java`\n\n```java\n// Style bảng dữ liệu\nJTable table = new JTable(model);\nModernStyler.styleTable(table);\nJScrollPane scrollPane = new JScrollPane(table);\nModernStyler.styleScrollPane(scrollPane);\n\n// Style panel\nJPanel panel = new JPanel();\nModernStyler.stylePanel(panel);\n\n// Tạo panel form\nJPanel formPanel = ModernStyler.createFormPanel();\n\n// Tạo section panel\nJPanel sectionPanel = ModernStyler.createSectionPanel(\"Tiêu đề\");\n\n// Style label\nJLabel label = new JLabel(\"Text\");\nModernStyler.styleLabel(label);\n```\n\n---\n\n## 🏗️ Cấu Trúc Thư Mục\n\n```\nsrc/main/java/coffee/\n├── util/\n│   ├── ModernUITheme.java             ← Thêm mới\n│   ├── ModernButton.java              ← Thêm mới\n│   ├── ModernTextField.java           ← Thêm mới\n│   ├── ModernPasswordField.java       ← Thêm mới\n│   └── ModernStyler.java              ← Thêm mới\n├── view/\n│   ├── CafeManagementApp.java         ← Cập nhật\n│   └── screens/\n│       ├── LoginView.java             ← Cập nhật\n│       ├── ProductView.java           ← Cập nhật\n│       ├── TableView.java             ← Cập nhật\n│       ├── EmployeeView.java          ← Cập nhật\n│       ├── InvoiceView.java           ← Cập nhật\n│       └── ReportView.java            ← Cập nhật\n├── controller/\n├── model/\n├── service/\n└── dao/\n```\n\n---\n\n## 🚀 Cách Sử Dụng Từng Component\n\n### Sử Dụng ModernButton\n\n```java\nimport coffee.util.ModernButton;\nimport coffee.util.ModernUITheme;\n\n// Ví dụ 1: Nút Thêm (Xanh lá cây)\nJButton addButton = new ModernButton(\n    \"Thêm\",\n    ModernUITheme.SUCCESS_COLOR,      // #22C55E\n    ModernUITheme.TEXT_PRIMARY        // #F8FAFC\n);\n\n// Ví dụ 2: Nút Sửa (Xanh dương)\nJButton editButton = new ModernButton(\n    \"Sửa\",\n    ModernUITheme.INFO_COLOR,         // #3B82F6\n    ModernUITheme.TEXT_PRIMARY\n);\n\n// Ví dụ 3: Nút Xóa (Đỏ)\nJButton deleteButton = new ModernButton(\n    \"Xóa\",\n    ModernUITheme.DANGER_COLOR,       // #EF4444\n    ModernUITheme.TEXT_PRIMARY\n);\n\n// Ví dụ 4: Nút tùy chỉnh\nJButton customButton = new ModernButton(\n    \"Tùy chỉnh\",\n    ModernUITheme.ACCENT_PURPLE,      // #A855F7\n    ModernUITheme.TEXT_PRIMARY\n);\n\n// Thêm action listener\naddButton.addActionListener(e -> {\n    System.out.println(\"Nút Thêm được nhấn\");\n});\n```\n\n### Sử Dụng ModernTextField\n\n```java\nimport coffee.util.ModernTextField;\nimport coffee.util.ModernPasswordField;\n\n// Tạo trường nhập\nJTextField nameField = new ModernTextField(20);\nnameField.setText(\"Nhập tên...\");\n\n// Tạo trường mật khẩu\nJPasswordField passwordField = new ModernPasswordField(20);\n\n// Lấy giá trị\nString name = nameField.getText();\nchar[] password = passwordField.getPassword();\n```\n\n### Sử Dụng ModernStyler\n\n```java\nimport coffee.util.ModernStyler;\nimport coffee.util.ModernUITheme;\n\n// Style bảng\nJTable table = new JTable(tableModel);\nModernStyler.styleTable(table);\n\nJScrollPane scrollPane = new JScrollPane(table);\nModernStyler.styleScrollPane(scrollPane);\n\n// Tạo form panel\nJPanel formPanel = ModernStyler.createFormPanel();\nGridBagConstraints gbc = new GridBagConstraints();\ngbc.insets = new Insets(ModernUITheme.PADDING_LG, ModernUITheme.PADDING_LG, \n                        ModernUITheme.PADDING_LG, ModernUITheme.PADDING_LG);\n\nJLabel label = new JLabel(\"Tên sản phẩm:\");\nModernStyler.styleLabel(label);\nformPanel.add(label, gbc);\n```\n\n### Sử Dụng ModernUITheme\n\n```java\nimport coffee.util.ModernUITheme;\n\n// Lấy màu\nColor primary = ModernUITheme.PRIMARY_COLOR;\nColor bgColor = ModernUITheme.BG_PRIMARY;\nColor textColor = ModernUITheme.TEXT_PRIMARY;\n\n// Lấy font\nFont titleFont = ModernUITheme.FONT_TITLE;\nFont bodyFont = ModernUITheme.FONT_BODY;\n\n// Lấy padding constants\nint padding = ModernUITheme.PADDING_LG;\n\n// Lấy border radius\nint radius = ModernUITheme.BORDER_RADIUS_MD;\n\n// Sử dụng trong code\npanel.setBackground(ModernUITheme.BG_PRIMARY);\nlabel.setFont(ModernUITheme.FONT_HEADING);\nlabel.setForeground(ModernUITheme.TEXT_PRIMARY);\n```\n\n---\n\n## 🎨 Bảng Màu Tham Khảo\n\n### Màu Chính\n```java\nModernUITheme.PRIMARY_COLOR        // #6366F1 (Chàm)\nModernUITheme.PRIMARY_DARK         // #4F46E5 (Chàm đậm)\nModernUITheme.PRIMARY_LIGHT        // #A5B4FC (Chàm nhạt)\n```\n\n### Màu Trạng Thái\n```java\nModernUITheme.SUCCESS_COLOR        // #22C55E (Xanh lá cây)\nModernUITheme.WARNING_COLOR        // #FB923C (Cam)\nModernUITheme.DANGER_COLOR         // #EF4444 (Đỏ)\nModernUITheme.INFO_COLOR           // #3B82F6 (Xanh dương)\n```\n\n### Màu Nền\n```java\nModernUITheme.BG_PRIMARY           // #0F172A (Xanh đen tối)\nModernUITheme.BG_SECONDARY         // #1E293B (Xám đen)\nModernUITheme.BG_TERTIARY          // #334155 (Xám nhạt)\n```\n\n### Màu Chữ\n```java\nModernUITheme.TEXT_PRIMARY         // #F8FAFC (Trắng)\nModernUITheme.TEXT_SECONDARY       // #CBD5E1 (Xám nhạt)\nModernUITheme.TEXT_TERTIARY        // #94A3B8 (Xám)\n```\n\n---\n\n## 📋 Danh Sách Font\n\n```java\nModernUITheme.FONT_TITLE           // Segoe UI, Bold, 28px\nModernUITheme.FONT_HEADING         // Segoe UI, Bold, 18px\nModernUITheme.FONT_SUBHEADING      // Segoe UI, Bold, 14px\nModernUITheme.FONT_BODY            // Segoe UI, Plain, 12px\nModernUITheme.FONT_SMALL           // Segoe UI, Plain, 11px\nModernUITheme.FONT_BUTTON          // Segoe UI, Bold, 12px\n```\n\n---\n\n## 🔍 Ví Dụ Thực Tế\n\n### Ví Dụ 1: Tạo màn hình form\n\n```java\nimport coffee.util.*;\nimport javax.swing.*;\nimport java.awt.*;\n\npublic class MyFormView extends JPanel {\n    public final JTextField nameField = new ModernTextField(20);\n    public final JPasswordField passwordField = new ModernPasswordField(20);\n    public final JButton submitButton = new ModernButton(\n        \"Gửi\", \n        ModernUITheme.PRIMARY_COLOR, \n        ModernUITheme.TEXT_PRIMARY\n    );\n\n    public MyFormView() {\n        setBackground(ModernUITheme.BG_PRIMARY);\n        setLayout(new GridBagLayout());\n        \n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.insets = new Insets(ModernUITheme.PADDING_MD, ModernUITheme.PADDING_MD, \n                                ModernUITheme.PADDING_MD, ModernUITheme.PADDING_MD);\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        \n        JLabel nameLabel = new JLabel(\"Tên:\");\n        ModernStyler.styleLabel(nameLabel);\n        gbc.gridx = 0; gbc.gridy = 0;\n        add(nameLabel, gbc);\n        gbc.gridx = 1;\n        add(nameField, gbc);\n        \n        JLabel pwdLabel = new JLabel(\"Mật khẩu:\");\n        ModernStyler.styleLabel(pwdLabel);\n        gbc.gridx = 0; gbc.gridy = 1;\n        add(pwdLabel, gbc);\n        gbc.gridx = 1;\n        add(passwordField, gbc);\n        \n        gbc.gridx = 1; gbc.gridy = 2;\n        add(submitButton, gbc);\n    }\n}\n```\n\n### Ví Dụ 2: Style một bảng dữ liệu\n\n```java\nJTable table = new JTable(tableModel);\nModernStyler.styleTable(table);\n\nJScrollPane scrollPane = new JScrollPane(table);\nModernStyler.styleScrollPane(scrollPane);\n\nJPanel panel = new JPanel(new BorderLayout());\npanel.setBackground(ModernUITheme.BG_PRIMARY);\npanel.add(scrollPane, BorderLayout.CENTER);\n```\n\n---\n\n## ⚙️ Cấu Hình Toàn Cục (UIManager)\n\nĐã được áp dụng trong `CafeManagementApp.java`:\n\n```java\nprivate void applyModernTheme() {\n    UIManager.put(\"Panel.background\", ModernUITheme.BG_PRIMARY);\n    UIManager.put(\"Panel.foreground\", ModernUITheme.TEXT_PRIMARY);\n    UIManager.put(\"Label.foreground\", ModernUITheme.TEXT_PRIMARY);\n    UIManager.put(\"TabbedPane.background\", ModernUITheme.BG_PRIMARY);\n    UIManager.put(\"TabbedPane.foreground\", ModernUITheme.TEXT_PRIMARY);\n    UIManager.put(\"Button.foreground\", ModernUITheme.TEXT_PRIMARY);\n}\n```\n\n---\n\n## ✅ Kiểm Tra Danh Sách Cài Đặt\n\n- ✅ Tạo 5 tệp utility mới (ModernUITheme, ModernButton, ModernTextField, ModernPasswordField, ModernStyler)\n- ✅ Cập nhật 6 view files (LoginView, ProductView, TableView, EmployeeView, InvoiceView, ReportView)\n- ✅ Cập nhật CafeManagementApp.java\n- ✅ Áp dụng tema toàn cục với UIManager\n- ✅ Tất cả các component sử dụng màu sắc hiện đại\n- ✅ Hover effects trên các nút\n- ✅ Focus effects trên các trường nhập\n- ✅ Consistent spacing và padding\n- ✅ Professional typography\n\n---\n\n## 🐛 Xử Lý Sự Cố\n\n### Nút không hiển thị đúng?\n- Kiểm tra rằng bạn đang import `ModernButton` từ `coffee.util`\n- Đảm bảo bạn đang sử dụng `ModernUITheme.XXX_COLOR` cho màu\n\n### Trường nhập không nhìn thấy?\n- Kiểm tra rằng panel cha có `setOpaque(false)` hoặc nền phù hợp\n- Đảm bảo font không quá nhỏ\n\n### Bảng không được style?\n- Gọi `ModernStyler.styleTable(table)` sau khi tạo table\n- Gọi `ModernStyler.styleScrollPane(scrollPane)` cho scroll pane\n\n### Màu chữ không nhìn thấy?\n- Kiểm tra nền màu, đảm bảo có độ tương phản cao đủ\n- Sử dụng `ModernUITheme.TEXT_PRIMARY` cho chữ trên nền tối\n\n---\n\n## 📞 Hỗ Trợ Thêm\n\nNếu bạn muốn:\n- **Thêm màu mới**: Chỉnh sửa `ModernUITheme.java`\n- **Tạo component mới**: Tham khảo `ModernButton.java` làm template\n- **Thay đổi khoảng cách**: Sửa các hằng số PADDING trong `ModernUITheme.java`\n- **Thay đổi font**: Chỉnh sửa các hằng số FONT trong `ModernUITheme.java`\n\n---\n\n**Tao bởi**: Thiết Kế Giao Diện Hiện Đại\n**Ngày**: 20/04/2026\n**Phiên bản**: 1.0\n"