# 🎯 Bảng Tham Khảo Nhanh - Thiết Kế Hiện Đại\n\n## 🎨 Màu Sắc Nhanh\n\n```java\n// Màu chính\nModernUITheme.PRIMARY_COLOR        // #6366F1 (Chàm) - Nút chính\nModernUITheme.SUCCESS_COLOR        // #22C55E (Xanh lá) - Nút Thêm\nModernUITheme.DANGER_COLOR         // #EF4444 (Đỏ) - Nút Xóa\nModernUITheme.INFO_COLOR           // #3B82F6 (Xanh dương) - Nút sửa\nModernUITheme.WARNING_COLOR        // #FB923C (Cam) - Cảnh báo\n\n// Nền\nModernUITheme.BG_PRIMARY           // #0F172A - Nền chính\nModernUITheme.BG_SECONDARY         // #1E293B - Panel\nModernUITheme.BG_TERTIARY          // #334155 - Trường nhập\n\n// Chữ\nModernUITheme.TEXT_PRIMARY         // #F8FAFC - Chữ chính\nModernUITheme.TEXT_SECONDARY       // #CBD5E1 - Chữ phụ\nModernUITheme.TEXT_TERTIARY        // #94A3B8 - Chữ nhạt\n```\n\n## ✏️ Font Nhanh\n\n```java\nModernUITheme.FONT_TITLE           // Segoe UI, Bold, 28px\nModernUITheme.FONT_HEADING         // Segoe UI, Bold, 18px\nModernUITheme.FONT_SUBHEADING      // Segoe UI, Bold, 14px\nModernUITheme.FONT_BODY            // Segoe UI, Plain, 12px\nModernUITheme.FONT_SMALL           // Segoe UI, Plain, 11px\nModernUITheme.FONT_BUTTON          // Segoe UI, Bold, 12px\n```\n\n## 📏 Khoảng Cách Nhanh\n\n```java\nModernUITheme.PADDING_XS           // 4px\nModernUITheme.PADDING_SM           // 8px\nModernUITheme.PADDING_MD           // 12px\nModernUITheme.PADDING_LG           // 16px\nModernUITheme.PADDING_XL           // 24px\nModernUITheme.PADDING_XXL          // 32px\n```\n\n## 🔘 Tạo Nút\n\n```java\n// Nút Thêm (Xanh lá)\nJButton add = new ModernButton(\"Thêm\", ModernUITheme.SUCCESS_COLOR, ModernUITheme.TEXT_PRIMARY);\n\n// Nút Sửa (Xanh dương)\nJButton edit = new ModernButton(\"Sửa\", ModernUITheme.INFO_COLOR, ModernUITheme.TEXT_PRIMARY);\n\n// Nút Xóa (Đỏ)\nJButton delete = new ModernButton(\"Xóa\", ModernUITheme.DANGER_COLOR, ModernUITheme.TEXT_PRIMARY);\n\n// Nút tùy chỉnh\nJButton custom = new ModernButton(\"Tên\", ModernUITheme.PRIMARY_COLOR, ModernUITheme.TEXT_PRIMARY);\n```\n\n## 📝 Tạo Trường Nhập\n\n```java\nJTextField field = new ModernTextField(20);\nJPasswordField pwd = new ModernPasswordField(20);\n```\n\n## 🎨 Style Bảng\n\n```java\nJTable table = new JTable(model);\nModernStyler.styleTable(table);\n\nJScrollPane scroll = new JScrollPane(table);\nModernStyler.styleScrollPane(scroll);\n```\n\n## 📋 Tạo Form\n\n```java\nJPanel form = ModernStyler.createFormPanel();\nGridBagConstraints gbc = new GridBagConstraints();\ngbc.insets = new Insets(ModernUITheme.PADDING_MD, ModernUITheme.PADDING_MD, \n                        ModernUITheme.PADDING_MD, ModernUITheme.PADDING_MD);\n\nJLabel label = new JLabel(\"Nhãn\");\nModernStyler.styleLabel(label);\nform.add(label, gbc);\n```\n\n## 📍 Import Cần Thiết\n\n```java\nimport coffee.util.ModernButton;\nimport coffee.util.ModernTextField;\nimport coffee.util.ModernPasswordField;\nimport coffee.util.ModernUITheme;\nimport coffee.util.ModernStyler;\n\nimport javax.swing.*;\nimport java.awt.*;\n```\n\n## 🔄 Cấu Trúc Điển Hình\n\n```java\npublic class MyView extends JPanel {\n    public MyView() {\n        // Setup\n        setBackground(ModernUITheme.BG_PRIMARY);\n        setLayout(new BorderLayout());\n        \n        // Component\n        JButton btn = new ModernButton(\"Nhấn\", ModernUITheme.PRIMARY_COLOR, ModernUITheme.TEXT_PRIMARY);\n        JTextField field = new ModernTextField(20);\n        \n        // Add\n        add(btn, BorderLayout.CENTER);\n        add(field, BorderLayout.SOUTH);\n    }\n}\n```\n\n## 🎯 Nút 3 Màu (Tiêu Chuẩn)\n\n```java\nJButton addBtn = new ModernButton(\"✅ Thêm\", ModernUITheme.SUCCESS_COLOR, ModernUITheme.TEXT_PRIMARY);\nJButton editBtn = new ModernButton(\"📝 Sửa\", ModernUITheme.INFO_COLOR, ModernUITheme.TEXT_PRIMARY);\nJButton delBtn = new ModernButton(\"🗑️ Xóa\", ModernUITheme.DANGER_COLOR, ModernUITheme.TEXT_PRIMARY);\n```\n\n## 📱 Kích Thước Chuẩn\n\n```java\n// Nút: 120x40px (tự động)\n// Trường nhập: 250x40px (tự động)\n// Bảng: Row height 32px\n// Window: 1000x700px\n```\n\n## 🌈 Hiệu Ứng\n\n```\n✨ Nút\n  - Hover: Sáng 10%\n  - Cursor: Hand\n  - Border: 8px bo tròn\n  - Padding: 5px\n\n✨ Trường\n  - Focus: Khung xanh 2px\n  - Blur: Khung xám 1px\n  - Caret: Xanh chàm\n  - Background: Xám nhạt\n\n✨ Bảng\n  - Header: Underline xanh chàm\n  - Selection: Xanh chàm\n  - Row height: 32px\n```\n\n## 🔍 Kiểm Tra Nhanh\n\n- ✅ Import đúng?\n- ✅ Set background = BG_PRIMARY?\n- ✅ Set foreground = TEXT_PRIMARY?\n- ✅ Set font = FONT_BODY?\n- ✅ Sử dụng ModernButton?\n- ✅ Sử dụng ModernTextField?\n- ✅ Gọi styleTable()?\n- ✅ Gọi stylePanel()?\n\n## 🐛 Gỡ Lỗi\n\n| Vấn Đề | Nguyên Nhân | Giải Pháp |\n|--------|----------|----------|\n| Nút không hiển thị | Không import ModernButton | Import `coffee.util.ModernButton` |\n| Chữ không nhìn thấy | Màu nền sai | Sử dụng `BG_PRIMARY` cho nền |\n| Trường không focus | Không gọi styleLabel | Gọi `ModernStyler.styleLabel()` |\n| Bảng không style | Không gọi styleTable | Gọi `ModernStyler.styleTable()` |\n| Hover không hoạt động | Tắt repainting | Kiểm tra `isHovered` được set |\n\n## 🧩 Mẫu Nhanh\n\n### Tạo Nút Group\n```java\nJPanel btnPanel = new JPanel(new FlowLayout(FlowLayout.RIGHT));\nbtnPanel.setBackground(ModernUITheme.BG_SECONDARY);\nbtnPanel.add(new ModernButton(\"Thêm\", ModernUITheme.SUCCESS_COLOR, ModernUITheme.TEXT_PRIMARY));\nbtnPanel.add(new ModernButton(\"Sửa\", ModernUITheme.INFO_COLOR, ModernUITheme.TEXT_PRIMARY));\nbtnPanel.add(new ModernButton(\"Xóa\", ModernUITheme.DANGER_COLOR, ModernUITheme.TEXT_PRIMARY));\n```\n\n### Tạo Panel Form\n```java\nJPanel form = ModernStyler.createFormPanel();\nGridBagConstraints gbc = new GridBagConstraints();\ngbc.insets = new Insets(ModernUITheme.PADDING_MD, ModernUITheme.PADDING_MD,\n                        ModernUITheme.PADDING_MD, ModernUITheme.PADDING_MD);\ngbc.fill = GridBagConstraints.HORIZONTAL;\n```\n\n### Tạo Bảng Styled\n```java\nJTable table = new JTable(model);\nModernStyler.styleTable(table);\nJScrollPane scroll = new JScrollPane(table);\nModernStyler.styleScrollPane(scroll);\nadd(scroll, BorderLayout.CENTER);\n```\n\n---\n\n**Bảng tham khảo nhanh v1.0**  \n**Cập nhật: 20/04/2026**\n"